import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function ScanPage() {
  return (
    <PagePlaceholder
      title="Log something"
      phase="Phase 3"
      description="Pick how to add an entry: scan a drink label, scan food, upload a medical report, or enter manually. You always review before saving."
    />
  );
}
