import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function ScanManualPage() {
  return (
    <PagePlaceholder
      title="Add manually"
      phase="Phase 5"
      description="Food / drink / water tabs with the right fields for each, plus save-to-favorites for fast re-logging."
    />
  );
}
