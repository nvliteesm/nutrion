import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function ScanFoodPage() {
  return (
    <PagePlaceholder
      title="Scan food"
      phase="Phase 8 (stretch)"
      description="Snap a meal, review detected items and portions, and save an AI estimate shown as a range."
    />
  );
}
