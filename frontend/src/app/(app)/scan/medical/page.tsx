import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function ScanMedicalPage() {
  return (
    <PagePlaceholder
      title="Upload medical report"
      phase="Phase 10 (stretch)"
      description="Premium: extract lab metrics, confirm each value, and receive cautious, educational nutrition-linked context. Never a diagnosis."
    />
  );
}
