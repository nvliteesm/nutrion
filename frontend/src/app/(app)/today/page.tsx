import { PagePlaceholder } from "@/components/layout/PagePlaceholder";

export default function TodayPage() {
  return (
    <PagePlaceholder
      title="Today"
      phase="Phase 2"
      description="Your daily dashboard — calorie ring, sugar, hydration, carbs, quick actions, recent entries and one supportive insight."
    />
  );
}
